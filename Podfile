# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'AlQuran' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for AlQuran
  pod 'Alamofire', '~> 5.1'
  pod 'SwiftyJSON', '~> 4.0'
  pod 'Pageable'

  target 'AlQuranTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'AlQuranUITests' do
    # Pods for testing
  end

end
